# Git
.git/
.gitignore
.gitattributes

# Azure and deployment
.azure/
azure.yaml
infra/
DEPLOYMENT.md
DEPLOYMENT_QUICKSTART.md
*.bicep
*.bicepparam

# Python virtual environments and caches
.venv/
venv/
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.so
*.egg
*.egg-info/
dist/
build/
.eggs/

# Python development
.pytest_cache/
.coverage
htmlcov/
.tox/
.mypy_cache/
.ruff_cache/
.pytype/

# Frontend (exclude heavy dev files, but keep source for build)
frontend/node_modules/
frontend/dist/
frontend/.vite/
frontend/.turbo/

# Node modules
backend/utils/node_modules/

# Environment files (secrets should not be in image)
.env
.env.*
!.env.example
!.env.azure.example

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Cache directories
cache/
.cache/

# IDE and editor files
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# Documentation (not needed at runtime)
README.md
*.md
!backend/**/*.md
docs/

# Scripts: most are dev-only, but the hierarchy-linker retrain runner is
# packaged in the image (see Dockerfile COPY scripts/ ./scripts/). Keep the
# directory available to the build context; runtime trims happen in the
# Dockerfile itself if needed.
start_backend.bat
*.ps1
*.sh

# Testing
tests/
test/
*.test.py
*.spec.py

# Data directories (exclude large files not needed in container)
data/*
!data/output/
# data_ingestion/ holds the Python modules used at runtime by the hierarchy
# linker cascade and the postprovision migrations — do NOT exclude.

# GraphRAG config (copy only what's needed in Dockerfile explicitly)
graphrag_config/cache/
graphrag_config/logs/

# Lock files (dependencies are already installed)
uv.lock
package-lock.json
yarn.lock
pnpm-lock.yaml

# Temporary files
*.tmp
*.temp
*.bak
*.swp
